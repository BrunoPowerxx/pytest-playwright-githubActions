import concurrent.futures
from betbots import *

def bet_sbp(item): # test 2
   with concurrent.futures.ProcessPoolExecutor as e:
       if __name__ = __main__:
           e.submit(sb_home, item)
           e.submit(bu_draw, item)
           e.submit(pb_away, item)
           
def bet_spb(item): # test 2
   with concurrent.futures.ProcessPoolExecutor as e:
       if __name__ = __main__:
           e.submit(sb_home, item)
           e.submit(pb_draw, item)
           e.submit(bu_away, item)

def bet_bps(item): # test 2
   with concurrent.futures.ProcessPoolExecutor as e:
       if __name__ = __main__:
           e.submit(bu_home, item)
           e.submit(pb_draw, item)
           e.submit(sb_away, item)

def bet_bsp(item): # test 2
   with concurrent.futures.ProcessPoolExecutor as e:
       if __name__ = __main__:
           e.submit(bu_home, item)
           e.submit(sb_draw, item)
           e.submit(pb_away, item)

def bet_pbs(item): # test 2
   with concurrent.futures.ProcessPoolExecutor as e:
       if __name__ = __main__:
           e.submit(pb_home, item)
           e.submit(bu_draw, item)
           e.submit(sb_away, item)

def bet_psb(item): # test 2
   with concurrent.futures.ProcessPoolExecutor as e:
       if __name__ = __main__:
           e.submit(pb_home, item)
           e.submit(sb_draw, item)
           e.submit(bu_away, item)
